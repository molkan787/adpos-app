const config = {
    appName: 'ADPOS',
    versionCode: 10107,
    versionName: '1.1.7',
    dev: false,
}

Object.freeze(config)
global.config = config;
window.config = config;
export default config;